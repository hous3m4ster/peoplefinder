"use strict";

/************
 * Requires *
 ************/

var FileMessage = require("./file_message");

/**************************
 * The PhotoMessage class *
 **************************/

/**
 * A message which contains a downloadable photo file
 *
 * @extends FileMessage
 */
class PhotoMessage extends FileMessage {
	/**
	 * Creates a new PhotoMessage object
	 *
	 * @param {Object} message	A raw message object from an update
	 * @param {BotAPI} API	An instance of a BotAPI from teleapiwrapper which will be used to do stuff with this message
	 *
	 * @private
	 */
	constructor(message, API) {
		// Call the super constructor
		super(message, API);
	}

	// AFAIK the last in the array is the original
	/**
	 * The received photo, as a [PhotoSize]{@link https://core.telegram.org/bots/api#photosize} object
	 *
	 * @type {Object}
	 * @property {String} file_id	ID of the photo
	 * @property {Integer} width	Width of the photo, in pixels
	 * @property {Integer} height	Height of the photo, in pixels
	 * @property {Integer} [file_size]	Size of the photo, in bytes
	 * @readonly
	 */
	get photo() {
		let photoArr = this.rawMessage.photo;
		return photoArr[photoArr.length-1];
	}
}

/***********
 * Exports *
 ***********/

module.exports = PhotoMessage;
