"use strict";

/************
 * Requires *
 ************/

var Message = require("./message");

/*********************************
 * The DeleteChatPhotoMessage class *
 *********************************/

/**
 * A message which indicates the photo of the chat has been deleted
 *
 * @extends Message
 */
class DeleteChatPhotoMessage extends Message {
	/**
	 * Creates a new DeleteChatPhotoMessage object
	 *
	 * @param {Object} message	A raw message object from an update
	 * @param {BotAPI} API	An instance of a BotAPI from teleapiwrapper which will be used to do stuff with this message
	 *
	 * @private
	 */
	constructor(message, API) {
		// Call the super constructor
		super(message, API);
	}
}

/***********
 * Exports *
 ***********/

module.exports = DeleteChatPhotoMessage;
