"use strict";

/************
 * Requires *
 ************/

var Message = require("./message");

/*************************************
 * The GroupChatCreatedMessage class *
 *************************************/

/**
 * A message which indicates a group chat has been created
 *
 * @extends Message
 */
class GroupChatCreatedMessage extends Message {
	/**
	 * Creates a new GroupChatCreatedMessage object
	 *
	 * @param {Object} message	A raw message object from an update
	 * @param {BotAPI} API	An instance of a BotAPI from teleapiwrapper which will be used to do stuff with this message
	 *
	 * @private
	 */
	constructor(message, API) {
		// Call the super constructor
		super(message, API);
	}
}

/***********
 * Exports *
 ***********/

module.exports = GroupChatCreatedMessage;
